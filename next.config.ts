import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /* config options here */
  // Vercel Build Trigger: Force update to pick up Razorpay fix
};

export default nextConfig;
